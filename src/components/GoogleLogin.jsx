import React from 'react';

function GoogleLogin(props) {

    const router =userRou

    const googleAuth = () => {
        
    }

    return (
        <div>

            <button onClick={googleAuth} > Signin with Google </button>
            
        </div>
    );
}

export default GoogleLogin;